import React from 'react';
import { Box } from '@mui/material';

const MainMovieList = () => {
    return (
        <Box
            sx={{
                background: '#aafdfd',
                height: '30vh',
                width: '300px',
            }}
        />
    );
};

export default MainMovieList;
