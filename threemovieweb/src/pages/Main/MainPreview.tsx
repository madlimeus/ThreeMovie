import React from 'react';
import { Box } from '@mui/material';

const MainPreview = () => {
    return (
        <Box
            sx={{
                background: 'skyblue',
                height: '60vh',
                width: '300px',
            }}
        />
    );
};

export default MainPreview;
